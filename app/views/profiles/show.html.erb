<%card = {
  header: "#{@profile.first_name} #{@profile.last_name}", 
  text: "
    <b>Téléphone</b> : #{@profile.phone_number} <br>
    <b>Adresse</b> : #{@profile.address}, #{@profile.address_complement}<br>
    <b>Ville</b> : #{@profile.city}<br>
    <b>Code postal</b> : #{@profile.zip_code}
  ",
  actions: [{label: "Éditer mon profil", path: edit_user_registration_path, type:"primary"}],
  footer: "Utilisateur actif depuis #{@profile.user.created_at}"
}%>

<%= render "partials/card", card: card%>

<%= render "orders/list", orders: @orders%>
