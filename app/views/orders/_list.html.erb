<div class="d-flex align-items-center justify-content-evenly bg-white" style="width: 80%">
  <%@orders.each do|order|%>
      <div>
        <h3 class="text-center m-3">Commande passée le <%=order.created_at.strftime("%d/%m/%Y")%></h3>
      </div>
      <div class="d-flex flex-column">
        <h4>ITEMS</h4>
        <%order.items.each do |item|%>
          <p><%=item.title%></p>
        <%end%>
      </div>  
  <%end%>
</div>
